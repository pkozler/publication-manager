namespace Core
{
    /// <summary>
    /// Abstraktní třída, která slouží k obecnému označení entit představujících
    /// doplňkové informace o publikacích (specifické pro jednotlivé typy publikací)
    /// za účelem předávání těchto entit odpovídajícím specializovaným komponentám
    /// bez nutné znalosti typu dané publikace v ostatních komponentách.
    /// </summary>
    public abstract class ASpecificPublication
    {
        // zatím žádný kód (pouze značkovací třída)
    }
}
